# Setup

1. Install dependencies:
   ```bash
   pip install pandas plotly flask
   ```
# Grade 3 English Results Dashboard
Flask + Jinja2 dashboard for Grade 3 English section results.
# Choice of Stack
As I have more learning and experience in python so it was easy for me to build something in python and its frameworks
# One real edge case
In app.py line 39 where pandas loads all the excel files. 
# AI usage
Used claude, first it provided the CDN link for charts etc then we figured it out using plotly. 
# Honest gap
It do not shows the cards of top performers and search option to see the dashboard of individual students. 
